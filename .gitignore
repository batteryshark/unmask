# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
build/
dist/

# uv
.uv/

# Local config / secrets (model endpoint + keys). Ship .env.example, never .env.
.env

# Tooling / OS
.DS_Store
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Run output (scans write here by default)
.mcd/

# Agent worktrees (temporary; separate git repos)
.claude/worktrees/
