# Transitional vendored scanner

`engine` + `mcd_lens` copied from `/Users/batteryshark/Downloads/runner-lab/parallax-goalpacks` (commit ff93fc6312385878fad0eda158b910d67cf611a8).

This is sloppy legacy code kept ONLY so the tool works while the native `unmask.scanner` is rebuilt slice by slice against tests/oracle. Delete this directory once the native source path passes the oracle. Do not build on it.
