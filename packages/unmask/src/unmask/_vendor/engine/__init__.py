"""Parallax engine + CLI (prlx).

Deterministic scanner over the data path:
inventory -> observation -> relationship -> lens -> report.

Observations are judgment-free (the ontology). Findings are lens-specific
interpretations. Severity and confidence are always separate.
"""

__version__ = "0.1.0"
SCANNER = "prlx"
